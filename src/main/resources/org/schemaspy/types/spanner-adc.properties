#
# Google Cloud Spanner with Application Default Credentials (ADC)
# see https://schemaspy.readthedocs.io/en/latest/configuration/databaseType.html
# for configuration / customization details
#

# Extends the base Spanner configuration
extends=spanner

# Description
description=Google Cloud Spanner using Application Default Credentials

# Connection specification for ADC
# No credentials parameter needed - the JDBC driver will automatically use ADC
connectionSpec=jdbc:cloudspanner:/projects/<project>/instances/<instance>/databases/<db>

# Parameter descriptions
project=Google Cloud Project ID
instance=Cloud Spanner instance ID
db=Cloud Spanner database name

# Prerequisites:
# 1. Install gcloud CLI: https://cloud.google.com/sdk/docs/install
# 2. Set up Application Default Credentials:
#    gcloud auth application-default login
# 3. Verify credentials:
#    gcloud auth application-default print-access-token
# 4. Ensure your account has required IAM roles:
#    - roles/spanner.databaseReader (minimum)
#    - roles/spanner.viewer (for instance metadata)

# Usage:
# java -jar schemaspy.jar \
#     -t spanner-adc \
#     -dp /path/to/google-cloud-spanner-jdbc.jar \
#     -cat YOUR_PROJECT_ID \
#     -db YOUR_DATABASE \
#     -s "" \
#     -o output_directory \
#     -connprops "instance\;YOUR_INSTANCE_ID"

# Notes:
# - ADC automatically uses credentials from gcloud
# - Works seamlessly in GCP environments (Cloud Run, GKE, Compute Engine, Cloud Functions)
# - No need to manage service account keys
# - Perfect for local development and GCP-hosted applications
